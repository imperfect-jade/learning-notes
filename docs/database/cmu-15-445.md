```
cd ~/DBMS/minisql

git checkout main
git pull

git checkout -b lab/buffer-pool

# 写代码

git diff
mkdir -p build
cd build
cmake ..
make -j$(nproc)
./test/lru_replacer_test
./test/buffer_pool_manager_test
cd ..

git add src/buffer src/include/buffer
git commit -m "implement buffer pool manager"
git push -u origin lab/buffer-pool

git checkout main
git merge lab/buffer-pool
git push

```
